import type { ChorusApprovalLabels } from './types';

export const DEFAULT_APPROVAL_LABELS: ChorusApprovalLabels = {
  title: 'Approval required',
  serverPrefix: 'via',
  inputLabel: 'Input',
  allowOnce: 'Allow once',
  allowAlways: 'Allow always for this tool',
  deny: 'Deny',
};
